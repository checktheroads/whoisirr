`whoisirr` is the most basic of bash scripts to query NTT's Internet Routing Registry mirror and return the results with some marginally fancy colors. It's little more than a `.bashrc` alias, really.

